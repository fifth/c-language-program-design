// 程序填空，不要改变与输入输出有关的语句。
// 一个物体从 100m 的高空自由落下，编写程序，求它在前 3s 内下落的垂直距离(结果保留2位小数)。设重力加速度为10米/秒^2。

#include <stdio.h>
int main(void)
{
    double height;

	height = 0.5*10*3*3;
    printf("height = %.2f\n", height);
}